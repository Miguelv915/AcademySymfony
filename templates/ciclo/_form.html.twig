{% import '@App/theme1/form.html.twig' as frm %}

{{ frm.start(form) }}
    {{ frm.columnX(
        [
            frm.row(form.precio),
            
        ],
        
    ) }}
    {{ frm.button(button_label|default('Guardar')) }}
{{ frm.end(form) }}
